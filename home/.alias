# PERMISSIONS
alias wp="wp --allow-root"

# GESTION SERVEUR
alias maj="apt-get update"
alias Maj="apt-get upgrade"
alias caddyMaj="~/config/tools/update-caddy.sh"
alias majConf="~/config/tools/majConf.sh"
alias db="~/config/tools/db.sh"

# Fichiers
alias duf='du -sh *'
alias df='df -h'
alias files="find . -type f | wc -l"

# LOGS
alias logs="tail -f /var/log/caddy/access.log"
alias syslog="tail -f /var/log/syslog"

# Services
alias status="systemctl status"
alias restart="systemctl restart"
alias reload="systemctl reload"
alias start="systemctl start"
alias stop="systemctl stop"